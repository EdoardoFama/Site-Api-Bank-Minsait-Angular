import { DepositoPipe } from './operacoes-extrato.pipe';

describe('OperacoesExtratoPipe', () => {
  it('create an instance', () => {
    const pipe = new DepositoPipe();
    expect(pipe).toBeTruthy();
  });
});
