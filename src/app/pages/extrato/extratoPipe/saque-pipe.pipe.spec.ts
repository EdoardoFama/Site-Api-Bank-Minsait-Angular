import { SaquePipePipe } from './saque-pipe.pipe';

describe('SaquePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SaquePipePipe();
    expect(pipe).toBeTruthy();
  });
});
